<?xml version="1.0" encoding="UTF-8" ?>
<?xaml-comp compile="true" ?>
<ResourceDictionary xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <Style TargetType="ContentPage" ApplyToDerivedTypes="True">
        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#FFF8F0, Dark=#1A1A1A}" />
    </Style>

    <Style TargetType="Shell" ApplyToDerivedTypes="True">
        <Setter Property="Shell.BackgroundColor" Value="{AppThemeBinding Light=#FFF5E8, Dark=#2E251D}" />
        <Setter Property="Shell.ForegroundColor" Value="{AppThemeBinding Light=#D4573A, Dark=#FF8A65}" />
        <Setter Property="Shell.TitleColor" Value="{AppThemeBinding Light=#D4573A, Dark=#FF8A65}" />
        <Setter Property="Shell.TabBarBackgroundColor" Value="{AppThemeBinding Light=#FFF5E8, Dark=#2E251D}" />
        <Setter Property="Shell.TabBarForegroundColor" Value="{AppThemeBinding Light=#D4573A, Dark=#FF8A65}" />
        <Setter Property="Shell.TabBarUnselectedColor" Value="Gray" />
    </Style>

    <Style TargetType="Button">
        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#D4573A, Dark=#FF6F4E}" />
        <Setter Property="TextColor" Value="White" />
        <Setter Property="FontSize" Value="{DynamicResource BodyFontSize}" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Padding" Value="14,10" />
        <Setter Property="VisualStateManager.VisualStateGroups">
            <VisualStateGroupList>
                <VisualStateGroup x:Name="CommonStates">
                    <VisualState x:Name="Normal" />
                    <VisualState x:Name="Disabled">
                        <VisualState.Setters>
                            <Setter Property="BackgroundColor" Value="LightGray" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateGroupList>
        </Setter>
    </Style>

    <Style TargetType="Entry">
        <Setter Property="FontSize" Value="{DynamicResource BodyFontSize}" />
        <Setter Property="PlaceholderColor" Value="Gray" />
        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#FFF, Dark=#333}" />
    </Style>

    <Style TargetType="Editor">
        <Setter Property="FontSize" Value="{DynamicResource BodyFontSize}" />
        <Setter Property="PlaceholderColor" Value="Gray" />
        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#FFF, Dark=#333}" />
    </Style>

    <Style TargetType="Picker">
        <Setter Property="FontSize" Value="{DynamicResource BodyFontSize}" />
    </Style>

    <Style TargetType="SearchBar">
        <Setter Property="FontSize" Value="{DynamicResource BodyFontSize}" />
        <Setter Property="PlaceholderColor" Value="Gray" />
        <Setter Property="BackgroundColor" Value="{AppThemeBinding Light=#FFF, Dark=#333}" />
    </Style>

    <Style TargetType="Label">
        <Setter Property="FontSize" Value="{DynamicResource BodyFontSize}" />
        <Setter Property="TextColor" Value="{AppThemeBinding Light=#333, Dark=#EEE}" />
    </Style>

    <Style TargetType="DatePicker">
        <Setter Property="FontSize" Value="{DynamicResource BodyFontSize}" />
    </Style>

</ResourceDictionary>
